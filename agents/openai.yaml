interface:
  display_name: "Multi-Agent Development"
  short_description: "按 FAST / STANDARD / ORCHESTRATED 渐进式编排 Codex 开发"
  default_prompt: "Use $multi-agent-development with progressive governance: choose the lightest sufficient FAST, STANDARD, or ORCHESTRATED workflow; make delegated tasks boundary-ready around outcome, scope, consequential architectural constraints, non-goals, and validation; route bounded cost-sensitive implementation to GPT-5.6 Terra and long-horizon, cross-system, materially ambiguous, or repeatedly non-convergent work to GPT-6 Astra when available; preserve useful role-owned context, increase Astra reasoning effort before replacing a valuable context when appropriate, and use parallel agents only when they materially shorten the critical path."
